# M3 / 3.7

Checkpoint acumulativo del **Curso Profesional de JasperReports 6.20.0 Community**.

- Parte exactamente de `M3/3.6`.
- Introduce Parameters y Variables sobre el informe de ventas ya corregido.
- Conserva el `LEFT JOIN`, los 14 títulos, los seis fields, la gestión de nulos y la paginación correcta de 3.6.
- Añade los parámetros `usuario` y `fechaInforme`.
- Añade las variables `TotalUnidades` y `TotalImporte`.
- Añade `PARAMETROS_VARIABLES.md`.

GitHub Actions valida este checkpoint en `.github/workflows/m3-e2e.yml`.
