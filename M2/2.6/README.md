# M2 / 2.6 — Expresiones

Checkpoint acumulativo que parte de `M2/2.5`.

Añade:

- parámetro `usuario`;
- variable `PrecioConIVA`;
- operadores aritméticos, lógicos, de comparación y ternarios;
- métodos de `String` y `Date`;
- combinación de `$F{}`, `$P{}` y `$V{}`;
- cinco expresiones calculadas en la segunda línea de Detail;
- `EXPRESIONES.md`.

El ancho útil del informe se mantiene en `columnWidth="555"`; todos los elementos de Detail quedan dentro de ese ancho. La banda Detail aumenta a 85 unidades para alojar la segunda fila sin solapamientos.

La carpeta es una solución de referencia. El alumno debe realizar primero el punto 2.6 sobre su propio resultado de 2.5.
